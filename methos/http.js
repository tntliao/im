import tools from '@/common/utils.js'
let util=tools.tools
let allHttp={
	getsinfo:()=>{
		// util.luch_get('index/Index/getsinfo').then(rs=>{
		// 	console.log(rs);
		// }).catch(err=>{
		// 	console.log(err);
		// })
	}
}
export {allHttp}